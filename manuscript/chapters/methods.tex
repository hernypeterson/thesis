\chapter{Methods}\label{methods}

\begin{figure}
{\centering
    \includegraphics[width=\textwidth]{study-area.pdf}
    \caption[Study Area: \acl{OM} Summit]{Study area at the summit of \acl{OM} in ArcGIS Pro. Sinusoidal Martian Projection. Contours in \unit{km}. Square is \qtyproduct{200 x 200}{\km}, centered at the midpoint of the \qty{19}{\km} contour.}\label{study-area}
}
\end{figure}

\section{Overview}
Goal (a) of this project is to constrain the degree and character of 3D surface deformation recorded at the summit of \ac{OM} to the greatest spatial extent possible. See Figure~\ref{study-area} for the spatial extent of the study area. 3D deformation is a characterization of net change; thus, initial and final surface attitude conditions are necessary. Attitude is completely defined by two angles; strike and dip are most widely used in geology. In this inquiry, dip direction will be used instead of strike because the lava flows naturally point in the dip direction of the surface upon which they are emplaced. No assumptions are made \textit{a priori} about large-scale spatial patterns in 3D deformation; therefore, independent calculations will be made at all locations where sufficient information is available.

Goal (b) of the project is to estimate magma reservoir inflation conditions (location, depth, size of reservoir, degree of inflation) consistent with observed 3D deformation at the surface. This process will entail running a series of numerical models simulating various possible combinations of magma reservoir conditions, analyzing the resultant surface deformation associated with each, and identifying the best match with the observed and calculated surface parameters.

While the technical definitions of these surface parameters will be presented later, it is worth noting here that 3D deformation discussed previously is a scalar term: in particular, the angular distance between the two normal vectors corresponding to each of the initial and final surfaces. This parameter plays two key roles in this project.

First, relevant to goal (a), it builds on the qualitative observations of \textcite{mouginis-mark_late-stage_2019} and extends the quantitative horizontal description of flow misalignment in \textcite{chadwick_late_2015} by accounting for vertical deformation immediately as a central component of the map-derived data collection.\footnote{Vertical deformation is accounted for in the geophysical modeling stage of \textcite{chadwick_late_2015}.} In other words, constraints on 3D deformation may be computed directly from the \ac{DEM} and mapped lava flows \& channels, prior to running numerical models. This strategy gives first-order insights by mapping regions where especially high magnitudes of 3D deformation are required to explain observed surface features. 

However, the 3D deformation parameter cannot simply be calculated for each point because insufficient attitude information is available for the paleosurface upon which lava flows and channels were originally emplaced. Recall that 3D deformation is the angular distance between the two normal vectors defining each of the initial and final surfaces. Each normal vector (equivalently, each surface attitude) requires two variables: dip and dip direction. Both of these are available for the modern (final) surface \ac{DEM}; however, only dip direction of the paleosurface can be inferred directly from the direction of lava flows. Rather than attempting to estimate paleoslope (dip) from lava flow morphology, this parameter is \textit{constrained.} Specifically, the paleo-dip assigned to each point answers the question: \textit{given modern dip, modern dip direction, and paleo-dip direction, what paleo-dip minimizes 3D deformation?} Through the simplifying assumption of minimum 3D deformation, two variable constraints (paleo-dip \& 3D deformation) can be obtained for the price of one, so to speak.

Second, relevant to goal (b), the previous discussion has shown how an assumption regarding of 3D deformation yields an estimate of paleo-dip which is independent of lava flow morphology (and can thus be compared with estimates derived from such methods). This is important because the numerical models used to test magma reservoir inflation events require construction of a paleo-edifice which no longer exists, and whose geometry must thus be inferred. Therefore, estimates of paleo-dip in addition to measurements of paleo-dip direction will guide first attempts at constructing this paleo-edifice.

To summarize, the 3D deformation strategy is to \begin{inparaenum}[a.]
    \item identify suitable point locations across the summit,
    \item collect attitude information for the modern surface at each point,
    \item infer paleoattitude information based on lava flows or channels at each point, and
    \item constrain total 3D deformation based on initial and final surface attitudes at each point. In this final step, an estimate of paleo-dip is produced as a additional useful result.
\end{inparaenum}
The following sections describe the details of each of these steps.

\section{Preparation of Published Data}
The \ac{CTX}\footnote{aboard the \ac{MRO} spacecraft launched in 2005 by \ac{NASA}.} captures \qty{\sim30}{\km} swaths across the entire martian surface in visible greyscale at \qty{\sim6}{\m} resolution. \textcite{Dickson2018AGB} blended these swaths to produce a raster mosaic product (hereafter, ``\ac{CTX} mosaic'') which is used here to visually identify lava flows and flow channels. In other words, this is the source from which paleo-dip direction is estimated across the summit region.

\ac{MOLA}\footnote{aboard the now-retired \ac{MGS} spacecraft launched in 1996 by \ac{NASA}.} returned topography data with horizontal resolution of \qtyproduct{300 x 1000}{\m} at the equator (better at high latitudes) and elevation uncertainty of \qty{\sim3}{\m}~\parencite{smith_mars_2001}. To improve spatial resolution, additional elevation data from the \ac{HRSC}\footnote{aboard the \ac{MEX} spacecraft launched in 2003 by the \ac{ESA}} was blended to product a \ac{DEM} with \qty{200}{\m} pixel resolution. Each pixel's vertical uncertainty is \qty{\sim1}{\m}, with an additional global uncertainty of \qty{\sim1.8}{\m} in the martian areoid (martian equivalent of Earth's geoid). In this project, the global areoid uncertainty is not a concern because only one region (the summit of \ac{OM}) is considered.

These two data sources were registered in an equal-area sinusoidal Mars projection in ArcGIS Pro. The study area is defined by a square \qtyproduct{200 x 200}{\km} centered at the centroid of the \qty{19}{\km} contour.\footnote{This is the highest integer \unit{km} which is roughly circular and completely encloses the caldera complex, implying that it largely records the conical shape of the shield edifice without influence from subsequent caldera collapse or reservoir inflation.} See Figure~\ref{study-area}.

\section{Mapping \& Sampling Site Selection}\label{mapping}
The \ac{CTX} mosaic was used to identify lava flows near the summit of \ac{OM}. Where possible, lobate flows were outlined and mapped as polygons. Flow centerlines were derived from these polygons, as shown in Figure~\ref{flow}. Channels were mapped as lines where flow margins were not visible. Some channels were discontinuous; these features were inferred to be regions where channels formed lava tubes which have subsequently collapsed to form a chain of skylights. See Figure~\ref{channel}.

\begin{figure}
{\centering
\begin{subfigure}{\textwidth}
    \centering
    \includegraphics[width=\textwidth]{flow.pdf}
    \caption[Mapped lava flow \& centerline]{A lobate flow is identified by relatively clear boundaries at each margin and mapped as a polygon (white) from which a centerline is derived for sampling.}\label{flow}
\end{subfigure}
\begin{subfigure}{\textwidth}
    \centering
    \includegraphics[width=\textwidth]{channel.pdf}
    \caption[Mapped lava channel]{A lava channel is mapped as a linear feature, including regions of discontinuity which are inferred to be collapsed skylight chains over lava tubes.}\label{channel}
\end{subfigure}
}
\caption{Mapping Linear Features}\label{mapping-linear}
\end{figure}


\begin{figure}
{\centering
        \includegraphics[width=\textwidth]{sampling.pdf}
        \caption[Sampling site selection]{Each linear feature is assigned an average paleo-dip direction. Points are selected for sampling and calculations at \qty{5}{\km} and \qty{3}{\km} intervals for flows and channels, respectively. Paleo-dip direction is assigned to each point from its corresponding line; modern dip and dip direction is assigned to each point from its unique \ac{DEM} neighborhood.}\label{sampling}
}
\end{figure}

In both cases, lava features are ultimately mapped as lines\footnote{Lines can have more than one vertex, i.e., they can be curved.} (rather than polygons) in ArcGIS Pro. While the \ac{DEM} covers the entire summit region, it is only along these lines that information about the paleosurface can be inferred. Therefore, points along these lines are selected as sampling locations where attitude measurements and calculations will take place. Points are produced at evenly spaced intervals of \qty{5}{\km} along each flow centerline and \qty{3}{\km} along each channel. No points are assigned to linear features of less than this length to ensure that flows and channels are long enough to record large-scale topography rather than local variation.

\section{Attitude Data Collection}
The ``Surface Parameters'' tool was used in ArcGIS Pro to compute average \acf{ze} and \acf{az} across the entire study area. In both cases, a new raster is produced at the same spatial resolution and coverage as the original \ac{DEM}. To avoid capturing local topographic anomalies, these values are averaged\footnote{Technically, a quadratic surface is interpolated over the neighborhood region and dip and dip direction are computed at the center of this surface.} over a circular ``neighborhood'' with radius \qty{2}{\km}. To each sampling location identified in Section~\ref{mapping} is assigned \acf{ze} and \acf{az} based on the value of the corresponding raster at that location. Figure~\ref{geometric-optimize-collected} shows a geometric view of the relevant data to be collected.

\section{Attitude Data Analysis}
\subsection{Minimum 3D Deformation}\label{3d-deform}
\begin{figure}
    \centering
    \input{figures/geometric-surface.tex}
    \caption[\Acl{az}-\acl{ze} representation of surfaces.]{The attitude of a surface is defined by two angles. In geology, strike and dip are the most common. In this thesis, slope (equivalent to dip) and downhill azimuth (\ang{90} from strike, equal to ``dip direction'') are used. For the mathematical procedures that follow, it will be convenient to describe surfaces by their normal (perpendicular) direction. In particular, unit vectors will be described in spherical coordinates: \acf{az} and \acf{ze}.}\label{geometric-surface}
\end{figure}
With a suitable representation of topographic surfaces described and a guiding set of assumptions established, the complexity of the goal at hand is reduced considerably. Namely, I calculate the slope of the paleo-surface upon which a lava flow was emplaced such that the net 3D deformation is minimized. Figure~\ref{geometric-optimize-calculated} shows a geometric view of the relevant data to be calculated and a conceptual model of the method used. The computational method is developed below.
\begin{figure}
    \centering
    \input{figures/geometric-optimize.tex}
    \caption{Geometric view of optimization method}\label{geometric-optimize}
\end{figure}
By the spherical law of cosines,\footnote{Equation~\ref{gcd} is also known as the ``great circle distance,'' since $r\acs{central-angle}$ gives the geodesic distance between two points on the surface of sphere with radius $r$.} the \acf{central-angle} between two vectors in spherical coordinates is:
\begin{equation}
    \acs{central-angle}=\arccos\left(\cos\acs{ze}\cos\acs{ze'}+\sin\acs{ze}\sin\acs{ze'}\cos\left|\acs{az'}-\acs{az}\right|\right).\label{gcd}
\end{equation}
For constant \acs{az}, \acs{ze}, and \acs{az'}, the value of \acs{ze'} which minimizes \acs{central-angle} can be calculated by setting:
\begin{equation}
    \frac{\partial}{\partial \acs{ze'}}\arccos\left(\cos\acs{ze}\cos\acs{ze'}+\sin\acs{ze}\sin\acs{ze'}\cos\left|\acs{az'}-\acs{az}\right|\right)=0.
\end{equation}
Differentiating using the chain rule,
\begin{equation}
    \frac{-\left(-\cos\acs{ze}\sin\ac{ze'}+\sin\acs{ze}\cos\ac{ze'}\cos\left|\acs{az'}-\acs{az}\right|\right)}{\sqrt{1-{\left(\cos\acs{ze}\cos\acs{ze'}+\sin\acs{ze}\sin\acs{ze'}\cos\left|\acs{az'}-\acs{az}\right|\right)}^2}}=0.
\end{equation}
Solving for \acs{ze'}:
\begin{gather}
    -\cos\acs{ze}\sin\ac{ze'}+\sin\acs{ze}\cos\ac{ze'}\cos\left|\acs{az'}-\acs{az}\right|=0,\nonumber\\
    \frac{\sin\acs{ze}\cos\left|\acs{az'}-\acs{az}\right|}{\cos\acs{ze}}=\frac{\sin\ac{ze'}}{\cos\ac{ze'}},\nonumber\\
    \boxed{\ac{ze'}=\arctan\left(\tan\ac{ze}\cos\left|\acs{az'}-\acs{az}\right|\right).}
\end{gather}
The calculated value of \acs{ze'} can be substituted back into Equation~\ref{gcd} to find \acs{central-angle}.

\subsection{Radial-Axial Component of 3D Deformation}
The unit normal vectors \acs{normal} and acs{normal'} have already been presented to justify the preceding mathematical treatment of surface attitude data, although their constituent angles in spherical coordinates have done all the computationally heavy lifting involved in calculating minimum 3D deformation. These vectors will play a more explicit role in the following section, in which I resolve the component of 3D deformation which occurs within the axial-radial plane. Two transformations involving these vectors are required. First, their expression in cartesian coordinates $(x,y,z)$:
\begin{equation}
    \acs{normal} = \begin{bmatrix}
        \sin\acs{ze}\cos\acs{az}\\
        \sin\acs{ze}\sin\acs{az}\\
        \cos\acs{ze}
    \end{bmatrix}\qquad
    \acs{normal'} = \begin{bmatrix}
        \sin\acs{ze'}\cos\acs{az'}\\
        \sin\acs{ze'}\sin\acs{az'}\\
        \cos\acs{ze'}
    \end{bmatrix}.\label{spherical-to-cartesian}
\end{equation}
Second, it is now necessary for the first time to consider the location of the sampled point. In particular, each sampled location is described in 2D polar coordinates $(\acs{R},\acs{THETA})$. The basis used thus far has been implicitly the standard basis:
\begin{equation}
    \hat x = \begin{bmatrix}
        1\\0\\0
    \end{bmatrix}\quad
    \hat y = \begin{bmatrix}
        0\\1\\0
    \end{bmatrix}\quad
    \hat z = \begin{bmatrix}
        0\\0\\1
    \end{bmatrix}.
\end{equation}
Notice, (e.g., in Figure~\ref{geometric-optimize}) only $\hat z$ has been used explicitly as the vertical direction; the actual directions assigned to $\hat x$ and $\hat y$ are not important because any orientation will yield the same value of $|\acs{az}'-\acs{az}|$. It is worth noting here, then, that azimuth angles are calculated throughout this thesis starting with $0$ in the vertical direction and increase clockwise. This means that unlike the typical representation in mathematics, $\hat x$ points up and $\hat y$ points to the right when viewing a map in the standard north-up orientation.

The axisymmetric modeling that follows requires a different choice of basis---one that depends on the location of point where the attitude data are collected and calculated. The zenith vector $\hat z$ is fine, but the horizontal vectors need to describe orientation in a radial or tangential direction, respectively. The following vectors form a basis:
\begin{equation}
    \hat r = \begin{bmatrix}
        \cos\acs{THETA}\\\sin\acs{THETA}\\0
    \end{bmatrix}\quad
    \hat t = \begin{bmatrix}
        -\sin\acs{THETA}\\\cos\acs{THETA}\\0
    \end{bmatrix}\quad
    \hat z = \begin{bmatrix}
        0\\0\\1
    \end{bmatrix}.
\end{equation}
Then a change-of-basis matrix \acs{change-basis} is constructed:
\begin{equation}
    \acs{change-basis}=\begin{bmatrix}
        \cos\acs{THETA} & -\sin\acs{THETA} & 0\\
        \sin\acs{THETA} & \cos\acs{THETA} & 0\\
        0 & 0 & 1
    \end{bmatrix}^{-1}=\begin{bmatrix}
        \cos\acs{THETA} & \sin\acs{THETA} & 0\\
        -\sin\acs{THETA} & \cos\acs{THETA} & 0\\
        0 & 0 & 1
    \end{bmatrix}.
\end{equation}
Thus the normal vectors \acs{normal} and \acs{normal'} can be expressed in radial-tangential form as \acs{change-basis}\acs{normal} and \acs{change-basis}\acs{normal'}, respectively. The component of 3D deformation (as minimized in Section~\ref{3d-deform}) in the radial-axial direction is therefore the angular distance between the projections of \acs{change-basis}\acs{normal} and \acs{change-basis}\acs{normal'} in the $r-z$ plane. We already have $z=\cos\ac{ze}$ from Equation~\ref{spherical-to-cartesian}. For $r$, we need just the first row of \acs{change-basis}:
\begin{equation}
    r=
    \begin{bmatrix}
        \cos\acs{THETA} & \sin\acs{THETA} & 0
    \end{bmatrix}\begin{bmatrix}
        \sin\acs{ze}\cos\acs{az}\\
        \sin\acs{ze}\sin\acs{az}\\
        \cos\acs{ze}
    \end{bmatrix}
    =\sin\acs{ze}\cos\acs{az}\cos\acs{THETA}+\sin\acs{ze}\sin\acs{az}\sin\acs{THETA}.
\end{equation}
Using the cosine of differences identity,
\begin{equation}
    r=\sin\acs{ze}\cos(\acs{THETA}-\acs{az}).
\end{equation}
A zenith angle can then be defined by
\begin{equation}
    \phi=\arctan(r/z)=\arctan\left[\tan\acs{ze}\cos(\acs{THETA}-\acs{az})\right].
\end{equation}
Likewise,
\begin{equation}
    \phi'=\arctan\left[\tan\acs{ze'}\cos(\acs{THETA}-\acs{az'})\right].
\end{equation}
Finally, the radial-axial component of deformation is
\begin{equation}
    \boxed{\acs{central-angle}_{rz}=\phi-\phi'.}
\end{equation}

\section{Axisymmetric Model of Reservoir Inflation}
\subsection{Proto-Edifice Reconstruction}
\subsection{Model Parameter Space}

\section{Crater Dating Lava Flows}
